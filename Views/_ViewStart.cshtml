@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    var isPublicPage = controller is "Home" or "Account";

    Layout = isPublicPage || User.Identity?.IsAuthenticated != true
        ? "_Layout"
        : "_PortalLayout";
}
